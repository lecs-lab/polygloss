[config]

mode = predict
pretrained_model = <PATH_TO_MODEL>  # TODO: Set the correct path to the pretrained model

# Dataset
dataset_key = lecslab/polygloss-corpus
batch_size = 64
task_format = concatenated
