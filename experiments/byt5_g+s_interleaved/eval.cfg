[config]

mode = predict
pretrained_model = <PATH_TO_MODEL>  # TODO: Set this to the correct model path before use

# Dataset
dataset_key = lecslab/polygloss-corpus
batch_size = 64
task_format = interleaved
